# SMARTY / Agentic Mathematical Reasoning environment
#
# Python: use the same Python environment for all packages.
# vLLM MUST be installed with uv and --torch-backend=auto because vLLM
# manages/selects the appropriate PyTorch backend during installation.
#
# Install vLLM separately:
#   uv pip install "vllm==0.27.1" --torch-backend=auto
#
# Then install this file:
#   uv pip install -r requirements.txt
#
# If an already-installed package has another version, uv pip will resolve
# it toward the versions/ranges specified below.

trl==1.12.0
datasets==5.0.1
bitsandbytes==0.50.2
peft==0.20.0
accelerate==1.14.0
sympy==1.14.0
matplotlib==3.10.0
huggingface_hub==1.28.0
torch==2.13.0
torchvision
torchaudio
torchao==0.18.0
numpy
pandas
antlr4-python3-runtime==4.11
graphviz>=0.20
markdown>=3.6

# Project/runtime utilities commonly required by the SMARTY scripts
transformers==5.16.1
