## What does this change?

## Why?

## Checklist (see README.md's Contributing section)
- [ ] `for f in *.py; do python3 -m py_compile "$f"; done` passes
- [ ] `for f in *.sh; do bash -n "$f"; done` passes
- [ ] New/changed logic has a test snippet demonstrating correct behavior
- [ ] No hardcoded paths, tokens, or host-specific assumptions
- [ ] README.md / IMPLEMENTATION.md updated if a flag, default, or file format changed
